// Local-only mode - no external terms/privacy links
export function TermsFooter({ className }: { className?: string }) {
  return (
    <p className={className}>
      Local-only mode - no data is sent to external servers.
    </p>
  );
}
